# handoff.yazi remote sync environment overrides
#
# This file is sourced by the `\r` key binding before `remote_sync.zsh` runs.
# Uncomment and customize any variable you want to override.
#
# Recommended approach:
# - Keep app/share settings in `config.lua`
# - Keep remote sync environment overrides in this file

# export HANDOFF_HOME="$HOME"
# export HANDOFF_SSH_CONFIG_PATH="$HOME/.ssh/config"
# export HANDOFF_STATE_ROOT="$HOME/.local/state"
# export HANDOFF_REMOTE_SYNC_DEBUG_LOG="${TMPDIR%/}/handoff-remote-sync-debug.log"
# export HANDOFF_REMOTE_SYNC_DEBUG=1
